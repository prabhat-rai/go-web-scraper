{{template "app" .}}
{{define "title"}}List{{end}}

{{define "main"}}
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Listing</h1>
    </div>
{{end}}